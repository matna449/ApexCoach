// F19.1 (#104): bare stub only -- Plan's real empty-state content (a
// calendar-shaped placeholder explaining that training-plan sync isn't
// connected yet) is #108, out of scope here. This ticket just reserves the
// nav slot and proves the route renders inside the new shell/palette.
function PlanView() {
  return (
    <div className="p-6">
      <h2 className="text-lg font-semibold">Plan</h2>
    </div>
  )
}

export default PlanView
